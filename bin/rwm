#!/bin/bash

audio=$1
if [[ $audio == "" ]] ; then
    audio=0
fi

out=`mktemp $HOME/webcam-XXXXXXXXXX.avi`

ffmpeg -f video4linux2 -r 25 -s 640x480 -i /dev/video0 \
    -f alsa -ac 2 -i hw:$audio,0 -acodec flac -y $out

echo $out

